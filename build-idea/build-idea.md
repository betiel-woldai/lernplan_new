
CC
concentrate on describing problem and solution to the next issue. explore and plan. think super hard as a senior developer to describe the issue. concentrate on describing problem and ssolution to the issue.dont do anything else and dont write code or act. 

Codex
concentrate on understanding and exploring following problem and solution to the next issue. explore and plan. think super hard as a senior developer to describe step by step solution plan. dont do anything else and dont write code or act. 


## Problems grob mit screenshot

<problem>
Phase A Schema

Add migration db/migrations/00x_calendar_session_metadata.sql to extend calendar_sessions with is_auto_generated BOOLEAN DEFAULT false, source_subject_exam_id UUID REFERENCES subjects(id), and scheduling_priority SMALLINT for conflict scoring.
Update src/lib/db.ts typings (if any query helpers rely on row types) to surface the new columns.
Extend CalendarSession in src/types/calendar.ts:6 with the new metadata flags plus a convenient origin union ('auto' | 'manual').
Document the new fields in docs/database.md:40 so other developers understand ownership semantics.
Regenerate seeds in src/scripts/seed.ts:337 ensuring study sessions insert the new columns, keeping manual fixtures untouched.
Phase B Scheduler

Replace generateStudySessionsForSubject in src/utils/calendarEventGenerator.ts:109 with a pure function that first returns abstract session requests (date + minutes + intensity flag) without times.
Create src/utils/scheduler/intelligentSessionScheduler.ts exporting a minimal buildStudySchedule(subjects, existingEvents) that calculates weekly buckets, applies intensity weeks, and yields sorted session slots with priority scores.
During generation inside generateCalendarEventsFromSubjects (src/utils/calendarEventGenerator.ts:19), call the new scheduler once per batch so conflicts are resolved with full visibility of already queued events.
Use priority scoring (distance to exam, intensity weeks) to order sessions before assigning times; assign sequential start times per day (e.g., 09:00 onward) while skipping collisions detected against both newly generated events and the input events array.
Mark auto-generated study sessions with isAutoGenerated, persist scheduling_priority, and mirror minutes into both plannedDuration and legacy duration fields.
Keep exam events in the same module but ensure they emit isAutoGenerated=true, source_subject_exam_id=subject.id, and scheduling_priority=0 so downstream code can treat them specially.
Phase C API Synchronization

Update subject create/update flows (src/pages/api/subjects/index.ts:156 and src/pages/api/subjects/[id].ts:199) to delete only is_auto_generated rows for that subject before inserting the refreshed schedule, preserving manual edits.
When inserting events in those handlers, populate the new metadata columns and rely on planned_duration instead of duration.
In src/pages/api/calendar/[id].ts:64, after the UPDATE, branch if the stored session is an exam: use the (possibly new) start_time to update subjects.exam_date and record updated_at, ideally inside a transaction to avoid skew.
Emit from the same handler a lightweight payload { subjectId, examDate } so the frontend can skip another fetch.
Adjust /api/calendar/sync.ts:16 to delete only auto-generated rows and to use the revamped scheduler, avoiding accidental removal of manual sessions during bulk syncs.
Phase D Frontend Sync

Extend useCalendarSessions.updateSession (src/hooks/useCalendarSessions.tsx:147) to detect when the edited session was an exam and dispatch a subjectExamDateChanged event carrying { subjectId, examDate }.
In useSubjects (src/hooks/useSubjects.tsx:88), listen for that custom event to patch the relevant subject in local state and mirror the date in ISO form to avoid a full refetch.
Update CalendarSessionEditModal (src/components/Calendar/CalendarSessionEditModal.tsx:33) to surface a read-only flag or warning when editing auto-generated sessions, while still allowing manual override.
When rendering sessions (src/components/Calendar/CalendarGrid.tsx et al.), visually hint auto-generated items (e.g., dotted border) using the new metadata so users understand which blocks will regenerate.
Ensure creation/edit modals default to sequential times by consulting the scheduler output, not hard-coded defaults.
Phase E Validation & Docs

Add unit coverage for the scheduler (new file under src/utils/scheduler/intelligentSessionScheduler.test.ts) to assert weekly distribution, intensity bumps, and conflict resolution for two overlapping subjects.
Create an integration test in tests/tests/calendar-sync.spec.ts that mutates an exam event through the calendar UI and verifies the subject modal reflects the new date.
Update docs/technical-implementation-flows.md:118 with the bi-directional exam sync diagram and note the event names used across hooks.
Run npm run lint, npm run type-check, and if Playwright runs are too heavy, at least execute the new spec locally to confirm deterministic scheduling.
Prep release notes describing schema migration requirements and advising developers to re-run npm run db:migrate plus npm run db:setup for clean datasets.

---

Phase 6 — Terminplan (Scaffold)

- Modules: src/lib/terminplan/{contracts,canonicalizer,diff,apply}.ts
- Reminders: src/lib/reminders/policy.ts (policy stubs)
- Popups: src/lib/notify/popup.ts (transport stub)
- Admin: src/pages/admin/terminplan.tsx (dry-run preview)
- Cron API: src/pages/api/cron/terminplan.ts (checksum + diff summary)

Usage (dev): open /admin/terminplan to preview dry-run against db/terminplan.json.

Next steps: persist versions, map entries → fixed events, schedule reminders.

---

Phase 7 — Hide “Termine & Fristen” Subject (Concise Plan)

1) Database
   - Add subjects.subject_type VARCHAR(20) DEFAULT 'academic'.
   - Backfill: set 'administrative' for per-user “Termine & Fristen”.
   - Index subject_type; ensure one admin subject per user (soft uniqueness by name + user).

2) Capability & Fallback
   - Add hasSubjectTypeColumn() check (like schema metadata helpers).
   - Fallback: if column missing, treat name == “Termine & Fristen” as administrative (server/client).

3) Terminplan Import
   - Ensure/create admin subject marked 'administrative' when column exists.
   - Reuse same admin subject per user; never expose in subject UI.

4) Subject APIs
   - List endpoints: filter out administrative subjects.
   - Mutations: block create/update/delete of administrative subject (409).
   - Fetch-by-id: 404 administrative subject to keep it invisible.

5) Calendar/Sync APIs
   - Skip administrative subjects for study/assignment generation.
   - Block creating user sessions tied to administrative subject.
   - Keep fixed terminplan events unchanged and immutable.

6) Analytics/Stats
   - Exclude administrative subjects and their sessions in all aggregates.
   - Ensure no XP/progress increments accrue to administrative subjects.

7) Client Hooks/UI
   - useSubjects: filter out administrative subjects (or name fallback).
   - Subject lists/dropdowns: rely on filtered hook; no UI special-casing.
   - Stats widgets: exclude fixed terminplan sessions from counts/minutes.
   - Calendar: continue displaying fixed terminplan events with toggle.

8) Migration & Backfill
   - Run schema migration → backfill admin classification.
   - Classify with dual signal: name match AND referenced by fixed events.
   - Rebuild caches/materialized views if any.

9) Validation
   - Subject UIs: admin subject absent; no errors.
   - Create session modal: admin subject not selectable; server blocks misuse.
   - Calendar: terminplan events visible, read-only, reminders OK.
   - Analytics: top-line metrics unchanged except exclusion of admin rows.

10) Rollout & Monitoring
   - Stage migration; smoke-test import, calendar, reminders.
   - Monitor for duplicate admin subjects and blocked deletes.
   - Keep fallback active until all envs have migrated.

11) Success Criteria
   - Admin subject never appears in subject UI or stats.
   - No study/XP/progress from administrative subjects.
   - Calendar terminplan events remain visible, fixed, and reminded.

</problem>


## Claude Code

## Codex

<solutionplan>

Phase A Schema

Add migration db/migrations/00x_calendar_session_metadata.sql to extend calendar_sessions with is_auto_generated BOOLEAN DEFAULT false, source_subject_exam_id UUID REFERENCES subjects(id), and scheduling_priority SMALLINT for conflict scoring.
Update src/lib/db.ts typings (if any query helpers rely on row types) to surface the new columns.
Extend CalendarSession in src/types/calendar.ts:6 with the new metadata flags plus a convenient origin union ('auto' | 'manual').
Document the new fields in docs/database.md:40 so other developers understand ownership semantics.
Regenerate seeds in src/scripts/seed.ts:337 ensuring study sessions insert the new columns, keeping manual fixtures untouched.
Phase B Scheduler

Replace generateStudySessionsForSubject in src/utils/calendarEventGenerator.ts:109 with a pure function that first returns abstract session requests (date + minutes + intensity flag) without times.
Create src/utils/scheduler/intelligentSessionScheduler.ts exporting a minimal buildStudySchedule(subjects, existingEvents) that calculates weekly buckets, applies intensity weeks, and yields sorted session slots with priority scores.
During generation inside generateCalendarEventsFromSubjects (src/utils/calendarEventGenerator.ts:19), call the new scheduler once per batch so conflicts are resolved with full visibility of already queued events.
Use priority scoring (distance to exam, intensity weeks) to order sessions before assigning times; assign sequential start times per day (e.g., 09:00 onward) while skipping collisions detected against both newly generated events and the input events array.
Mark auto-generated study sessions with isAutoGenerated, persist scheduling_priority, and mirror minutes into both plannedDuration and legacy duration fields.
Keep exam events in the same module but ensure they emit isAutoGenerated=true, source_subject_exam_id=subject.id, and scheduling_priority=0 so downstream code can treat them specially.
Phase C API Synchronization

Update subject create/update flows (src/pages/api/subjects/index.ts:156 and src/pages/api/subjects/[id].ts:199) to delete only is_auto_generated rows for that subject before inserting the refreshed schedule, preserving manual edits.
When inserting events in those handlers, populate the new metadata columns and rely on planned_duration instead of duration.
In src/pages/api/calendar/[id].ts:64, after the UPDATE, branch if the stored session is an exam: use the (possibly new) start_time to update subjects.exam_date and record updated_at, ideally inside a transaction to avoid skew.
Emit from the same handler a lightweight payload { subjectId, examDate } so the frontend can skip another fetch.
Adjust /api/calendar/sync.ts:16 to delete only auto-generated rows and to use the revamped scheduler, avoiding accidental removal of manual sessions during bulk syncs.
Phase D Frontend Sync

Extend useCalendarSessions.updateSession (src/hooks/useCalendarSessions.tsx:147) to detect when the edited session was an exam and dispatch a subjectExamDateChanged event carrying { subjectId, examDate }.
In useSubjects (src/hooks/useSubjects.tsx:88), listen for that custom event to patch the relevant subject in local state and mirror the date in ISO form to avoid a full refetch.
Update CalendarSessionEditModal (src/components/Calendar/CalendarSessionEditModal.tsx:33) to surface a read-only flag or warning when editing auto-generated sessions, while still allowing manual override.
When rendering sessions (src/components/Calendar/CalendarGrid.tsx et al.), visually hint auto-generated items (e.g., dotted border) using the new metadata so users understand which blocks will regenerate.
Ensure creation/edit modals default to sequential times by consulting the scheduler output, not hard-coded defaults.
Phase E Validation & Docs

Add unit coverage for the scheduler (new file under src/utils/scheduler/intelligentSessionScheduler.test.ts) to assert weekly distribution, intensity bumps, and conflict resolution for two overlapping subjects.
Create an integration test in tests/tests/calendar-sync.spec.ts that mutates an exam event through the calendar UI and verifies the subject modal reflects the new date.
Update docs/technical-implementation-flows.md:118 with the bi-directional exam sync diagram and note the event names used across hooks.
Run npm run lint, npm run type-check, and if Playwright runs are too heavy, at least execute the new spec locally to confirm deterministic scheduling.
Prep release notes describing schema migration requirements and advising developers to re-run npm run db:migrate plus npm run db:setup for clean datasets.

---

Phase 6 — Terminplan (Scaffold)

- Modules: src/lib/terminplan/{contracts,canonicalizer,diff,apply}.ts
- Reminders: src/lib/reminders/policy.ts (policy stubs)
- Popups: src/lib/notify/popup.ts (transport stub)
- Admin: src/pages/admin/terminplan.tsx (dry-run preview)
- Cron API: src/pages/api/cron/terminplan.ts (checksum + diff summary)

Usage (dev): open /admin/terminplan to preview dry-run against db/terminplan.json.

Next steps: persist versions, map entries → fixed events, schedule reminders.

---

Phase 7 — Hide “Termine & Fristen” Subject (Concise Plan)

1) Database
   - Add subjects.subject_type VARCHAR(20) DEFAULT 'academic'.
   - Backfill: set 'administrative' for per-user “Termine & Fristen”.
   - Index subject_type; ensure one admin subject per user (soft uniqueness by name + user).

2) Capability & Fallback
   - Add hasSubjectTypeColumn() check (like schema metadata helpers).
   - Fallback: if column missing, treat name == “Termine & Fristen” as administrative (server/client).

3) Terminplan Import
   - Ensure/create admin subject marked 'administrative' when column exists.
   - Reuse same admin subject per user; never expose in subject UI.

4) Subject APIs
   - List endpoints: filter out administrative subjects.
   - Mutations: block create/update/delete of administrative subject (409).
   - Fetch-by-id: 404 administrative subject to keep it invisible.

5) Calendar/Sync APIs
   - Skip administrative subjects for study/assignment generation.
   - Block creating user sessions tied to administrative subject.
   - Keep fixed terminplan events unchanged and immutable.

6) Analytics/Stats
   - Exclude administrative subjects and their sessions in all aggregates.
   - Ensure no XP/progress increments accrue to administrative subjects.

7) Client Hooks/UI
   - useSubjects: filter out administrative subjects (or name fallback).
   - Subject lists/dropdowns: rely on filtered hook; no UI special-casing.
   - Stats widgets: exclude fixed terminplan sessions from counts/minutes.
   - Calendar: continue displaying fixed terminplan events with toggle.

8) Migration & Backfill
   - Run schema migration → backfill admin classification.
   - Classify with dual signal: name match AND referenced by fixed events.
   - Rebuild caches/materialized views if any.

9) Validation
   - Subject UIs: admin subject absent; no errors.
   - Create session modal: admin subject not selectable; server blocks misuse.
   - Calendar: terminplan events visible, read-only, reminders OK.
   - Analytics: top-line metrics unchanged except exclusion of admin rows.

10) Rollout & Monitoring
   - Stage migration; smoke-test import, calendar, reminders.
   - Monitor for duplicate admin subjects and blocked deletes.
   - Keep fallback active until all envs have migrated.

11) Success Criteria
   - Admin subject never appears in subject UI or stats.
   - No study/XP/progress from administrative subjects.
   - Calendar terminplan events remain visible, fixed, and reminded.


</solutionplan>